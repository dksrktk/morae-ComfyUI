# Morae Pipeline Dependencies
# pip install -r morae_pipeline/requirements.txt

# ===== Core =====
torch>=2.0.0
numpy>=1.24.0
Pillow>=9.0.0
pyyaml>=6.0
requests>=2.28.0
aiohttp>=3.8.0

# ===== Image Processing =====
opencv-python>=4.8.0
scipy>=1.10.0

# ===== Detection (Censorship) =====
ultralytics>=8.0.0                    # YOLO
dghs-imgutils[gpu]>=0.8.0             # imgutils (detect_censors, nudenet)

# ===== Segmentation (Censorship) =====
# SAM2 - install from GitHub
# pip install git+https://github.com/facebookresearch/sam2.git

# ===== Curation =====
open_clip_torch>=2.20.0               # CLIP for aesthetic scoring
# Optional: transformers>=4.30.0      # Fallback CLIP if open_clip unavailable

# ===== Note =====
# ComfyUI dependencies (torch, numpy, etc.) are usually already installed.
# Only install the additional dependencies listed above.
#
# For SAM2:
#   pip install git+https://github.com/facebookresearch/sam2.git
#
# For imgutils with GPU:
#   pip install dghs-imgutils[gpu]
#
# Download required models:
#   - SAM2: models/sam2/sam2.1_hiera_large.pt
#   - YOLO: models/yolo/yolov8_seg.pt (auto-downloaded)
#   - Aesthetic: models/aesthetic/aesthetic_predictor_v2.pth
#     (from https://github.com/christophschuhmann/improved-aesthetic-predictor)
